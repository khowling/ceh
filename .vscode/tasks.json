{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd ${workspaceFolder}/cmake && cmake .. && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}